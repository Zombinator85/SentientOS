# Inactive intent-expansion examples. A matching machine-specific structured
# shell rule is still required; templates and placeholders grant no authority.
# name: action mapping
# e.g., restart service
restart_service:
  type: shell
  argv: ["systemctl", "restart", "{service}"]
list_files:
  type: shell
  argv: ["ls", "{path}"]
