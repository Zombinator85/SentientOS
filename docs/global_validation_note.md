# Global Green Validation + Debt Containment Note

This pass executes one comprehensive profile-aware protected corridor across constitutional,
forge, contract, simulation, formal, federation, operator, typing-ratchet, and audit surfaces.

Current release-corridor posture is green on blocking checks under `ci-advisory` and
`federation-enforce`, with explicit non-blocking visibility for:

- legacy typing debt outside protected mypy scopes
- optional historical/runtime audit chain split conditions

Deferred debt remains visible in `glow/contracts/protected_corridor_report.json` and should
not be interpreted as hidden or waived blocking failures.
