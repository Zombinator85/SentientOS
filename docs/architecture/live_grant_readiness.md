
## Evidence-backed runtime use

Runtime callers must satisfy prerequisites from exact validated evidence, not artifact presence alone. Schema grant records prove schema availability only; safety manifests do not prove all gates satisfied; reviewer proof bundles require validated current digest/status linkage; operator approval, policy approval, effect receipts, rollback receipts, postcondition checks, and local grant issuance are never inferred from metadata-only records.
