# SentientOS Repository Agent Map

SentientOS prioritizes operator accountability, auditability, and safe shutdown. No presence, privilege, emotion, track, clip, blessing, or action goes unwitnessed.

## Canonical doctrine

The Cathedral blessing, agent taxonomy and ledger, presence and privilege contracts, federation rules, procedures, and witnessing law are preserved in [`docs/AGENTS_DOCTRINE_ARCHIVE.md`](docs/AGENTS_DOCTRINE_ARCHIVE.md). Public governance doctrine is indexed by [`docs/GOVERNANCE_DOCTRINE.md`](docs/GOVERNANCE_DOCTRINE.md). Changes to agent privilege must cite the blessed origin and update the canonical ledger. Invocations of `bless`, `animate`, or `reflect` must log to `/logs/privileges/`.

## Operator and authority boundaries

- Preserve local operator authority and fail closed on ambiguous privileged action.
- Do not add provider invocation, provider SDKs, prompt export, network egress, external disclosure, or runtime provider authority unless explicitly requested.
- Do not add direct host actuation without control-plane admission, audit, rollback, panic, and operator authority. Phase 1 host resource work is read-only; never add fan/PWM/thermal writes without explicit authority.
- Federation evidence, receipts, readiness, proposals, metadata-only work, dry runs, sandboxes, and review evidence are not adoption, consent, policy truth, transport, sync, merge, apply, installation, execution, or live-memory mutation authority.
- Do not modify `prompt_assembler.py` unless explicitly asked. Never put try/catch around imports.
- Operate on `.py` and `.bat` files unless the task explicitly authorizes docs, tests, governance, or other surfaces. Do not edit this file except when explicitly authorized.

## Codex hot path

1. Require the prompt's fresh-current SHA and clean initial tree.
2. Bootstrap first. Stop immediately on `blocked`; `BLOCKED_DO_NOT_IMPLEMENT` artifacts are diagnostic only.
3. Implement a complete bounded task. Required validation failures are task-owned until proven otherwise; repair task-caused fallout in this workspace.
4. Run repository-native focused tests with `python -m scripts.run_tests`, targeted mypy, the relevant matrix, docs/prompt checks when applicable, strict audits, and immutability verification. Focused tests or aggregate counts alone are not behavioral proof.
5. Run the two-phase landing finalizer in the same implementation task. Pre-commit must return `ready_to_commit`; `unknown_dirty_tree` and `manual_review_required` block commit. Commit once. Post-commit must return `ready_for_pr_metadata`.
6. Require `pr_metadata_guard_ready`, generate the PR body from canonical artifacts, require `pr_body_binding_ready` for the exact bytes, recheck clean tree/current HEAD, then call `make_pr` once. A payload echo is unverified publication.
7. Never final-report partial sealing while feasible task-owned repair remains. If no repository change was made, do not commit or call `make_pr`.

The policy source is [`docs/development/codex_validation_and_landing_contract.md`](docs/development/codex_validation_and_landing_contract.md). The executable commands and finalizer statuses are canonical in [`docs/development/codex_finalize_landing.md`](docs/development/codex_finalize_landing.md). Do not copy their full ritual into templates, roadmaps, or prompts.

## Repository navigation

- Active work and sealed boundaries: [`docs/development/codex_open_work_roadmap_index.md`](docs/development/codex_open_work_roadmap_index.md)
- Whole-system task shape: [`docs/development/codex_whole_system_task_template.md`](docs/development/codex_whole_system_task_template.md)
- Narrow repair shape: [`docs/development/codex_narrow_repair_task_template.md`](docs/development/codex_narrow_repair_task_template.md)
- Validation and landing policy: [`docs/development/codex_validation_and_landing_contract.md`](docs/development/codex_validation_and_landing_contract.md)
- Executable landing reference: [`docs/development/codex_finalize_landing.md`](docs/development/codex_finalize_landing.md)
- Evidence recovery: [`docs/development/codex_landing_evidence_recovery_rail.md`](docs/development/codex_landing_evidence_recovery_rail.md)
- Capability checklist: [`docs/development/codex_capability_landing_checklist.md`](docs/development/codex_capability_landing_checklist.md)
- Memory-chain profiles: [`docs/development/codex_memory_chain_task_profile.md`](docs/development/codex_memory_chain_task_profile.md) and [`docs/development/codex_memory_chain_recovery_profile.md`](docs/development/codex_memory_chain_recovery_profile.md)

## Validation commands

Use the canonical command reference above for landing. Core contribution checks are:

- `python -m mypy scripts/ sentientos/` (or task-targeted scope where required; never modify the baseline to hide failures)
- `python -m scripts.run_tests -q`
- `python verify_audits.py --strict`
- `python scripts/audit_immutability_verifier.py`

Behavior-adding tasks must supply the versioned exact-node acceptance manifest described by the validation contract and must name at least one successful-path pytest node. Legacy tasks may omit it only under the documented migration rule.

## Commit and PR title

Use `[codex:<subsystem>] <intent summary>`.
